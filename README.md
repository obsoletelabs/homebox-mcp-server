# homebox-mcp-server
a homebox mcp server made by obsoletelabs



claud example
```
"mcpServers": {
    "my-server": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "http://{ip}:{port}/mcp",
	"--allow-http"
      ]
    }
```